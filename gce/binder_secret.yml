jupyterhub:
    hub:
      services:
        binder:
          apiToken: SECRET
    proxy:
      secretToken: SECRET
hub:
  services:
    binder:
      apiToken: SECRET
registry:
  password: |
    {
      SECRET
    }
